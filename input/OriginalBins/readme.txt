Here, a directory for every sample should be located with the name {sample}. Inside each OriginalBins/{sample}/ directory will contain all of the Bin.{number}.fasta files for the sample.
Note: Make sure each bin file follows the naming scheme listed above.
